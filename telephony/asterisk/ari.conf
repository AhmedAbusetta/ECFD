; ===================================================
; ECFD Asterisk REST Interface (ARI) Configuration
; ===================================================

[general]
enabled=yes
pretty=yes
allowed_origins=http://localhost:5000,http://localhost:3000

[ecfd_ari_admin]
type=user
read_only=no
password=dev_ari_password
password_format=plain
